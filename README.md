# tt-cli

To install dependencies:

```bash
bun install
```

To install:

```bash
bun run install.ts
```

Settings are located in `~/.config/tt-cli/settings.json`.

```json
{
    "notes_dir": "~/Documents/Notes"
}
```
